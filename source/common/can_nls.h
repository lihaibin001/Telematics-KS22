#ifndef __CAN_NLS_H__
#define __CAN_NLS_H__

#include "Debug.h"
#include "vehicle.h"

typedef struct{
	uint8_t *pData;
	struct{
		uint8_t type:2;
		uint8_t
	};
};

uint8_t can_nls_send(uint8_t *pData, uint8_t len, uint8_t )

#endif //__CANN_NLS_H__
